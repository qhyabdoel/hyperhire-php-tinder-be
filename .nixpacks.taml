[phases.setup]
nixPkgs = ['php83', 'php83Extensions.composer']

[phases.install]
cmds = [
    "composer install --optimize-autoloader --no-scripts --no-interaction"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT}"